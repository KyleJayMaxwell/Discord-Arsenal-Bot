{
  "name": "arsenal-bot",
  "version": "1.0.0",
  "description": "Arsenal FC match reminder bot — posts to Discord via webhook, runs on GitHub Actions",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test-webhook": "node index.js test-webhook"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  }
}